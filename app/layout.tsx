import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SHOUTDOWN — Voice-Controlled Falling Words",
  description:
    "Shout words to destroy them before they hit the bottom! A voice-controlled falling-words game inspired by Z-Type. Built with Next.js, Canvas, and Web Speech API.",
  keywords: ["voice game", "falling words", "speech recognition", "z-type", "browser game"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
